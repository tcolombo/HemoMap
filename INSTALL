# Mode debug
cordova/debug

# En cas de problème avec le serveur :
adb kill-server
sudo adb start-server
adb devices # pour vérifier




# Production
ant release

# Signer avec passphrase = f.......
jarsigner -keystore info2dev.keystore bin/HemoMap-release-unsigned.apk info2dev

# Optimisation du paquetage
zipalign -v 4 bin/HemoMap-release-unsigned.apk bin/HemoMap.apk

# Pour tester le paquetage
emulator -wipe-data -avd default &
adb install bin/HemoMap.apk
